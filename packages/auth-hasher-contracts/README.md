# packages/auth-hasher-contracts

Shared preset definitions, metadata types, and password hash assessment helpers for the internal
`AUTH_HASHER` Worker.

This package stays runtime-neutral so both the Worker shell and the application-facing client
helpers can depend on the same source of truth.
