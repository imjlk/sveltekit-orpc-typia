# packages/auth-hasher

Compatibility barrel for the internal auth hasher workspace packages.

Prefer importing from:

- `@repo/auth-hasher-contracts`
- `@repo/auth-hasher-client`
- `@repo/auth-hasher-better-auth-adapter`

This package remains only to keep older starter imports working while the repo aligns with the
newer template split.
